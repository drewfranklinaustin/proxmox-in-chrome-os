#!/bin/sh
sudo apt-get install wget nala nano gpg -f -y && sudo usermod -aG sudo drewfranklinaustin && sudo passwd root && sudo passwd drewfranklinaustin && wget -q -O - https://dl-ssl.google.com/linux>
sudo apt-get build-dep fuse fuse3
sudo dpkg -r  
sudo apt-get -f install
sudo aptitude install fuse fuse3
